<!-- Derived from AvaloniaUI/Avalonia 11.3.12 (MIT). See LICENSE.Avalonia.txt and UpstreamManifest.json. --><ResourceDictionary xmlns="https://github.com/avaloniaui" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:ClassModifier="internal">
  <Design.PreviewWith>
    <Border Padding="20">
      <StackPanel Spacing="20">
        <TabStripItem>Leaf</TabStripItem>
        <TabStripItem IsSelected="True">Arch</TabStripItem>
        <TabStripItem Background="Yellow">Background</TabStripItem>
      </StackPanel>
    </Border>
  </Design.PreviewWith>

  <x:Double x:Key="TabStripItemMinHeight">48</x:Double>
  <x:Double x:Key="TabStripItemPipeThickness">2</x:Double>

  <ControlTheme x:Key="{x:Type TabStripItem}" TargetType="TabStripItem" x:DataType="TabStripItem"><Setter Property="FontFamily" Value="{DynamicResource BrowserFontFamily}"/><Setter Property="CornerRadius" Value="{DynamicResource BrowserControlRadius}"/>
    <Setter Property="FontSize" Value="{DynamicResource BrowserFontSize}"/>
    <Setter Property="FontWeight" Value="{DynamicResource TabItemHeaderThemeFontWeight}"/>
    <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource BrowserTextBrush}"/>
    <Setter Property="Padding" Value="{DynamicResource TabItemHeaderMargin}"/>
    <Setter Property="Margin" Value="0"/>
    <Setter Property="MinHeight" Value="{DynamicResource BrowserControlHeight}"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <ControlTemplate>
        <Border Name="PART_LayoutRoot" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}" Padding="{TemplateBinding Padding}">
          <Panel>
            <ContentPresenter Name="PART_ContentPresenter" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}"/>
            <Border Name="PART_SelectedPipe" Background="{DynamicResource BrowserAccentBrush}" CornerRadius="{DynamicResource ControlCornerRadius}" Height="{DynamicResource TabStripItemPipeThickness}" IsVisible="False" Margin="0,0,0,2" VerticalAlignment="Bottom"/>
          </Panel>
        </Border>
      </ControlTemplate>
    </Setter>

    <!--  Selected state  -->
    <!--  We don't use selector to PART_LayoutRoot, so developer can override selected item background with TabStripItem.Background  -->
    <Style Selector="^:selected">
      <Setter Property="Background" Value="{DynamicResource BrowserSelectionBrush}"/>
      <Setter Property="Foreground" Value="{DynamicResource BrowserSelectionTextBrush}"/>
    </Style>
    <Style Selector="^:selected /template/ Border#PART_SelectedPipe">
      <Setter Property="IsVisible" Value="True"/>
    </Style>

    <!--  PointerOver state  -->
    <Style Selector="^:pointerover /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
      <Setter Property="TextElement.Foreground" Value="{DynamicResource BrowserTextBrush}"/>
    </Style>

    <!--  Selected PointerOver state  -->
    <Style Selector="^:selected:pointerover /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{DynamicResource BrowserSelectionBrush}"/>
      <Setter Property="TextElement.Foreground" Value="{DynamicResource BrowserSelectionTextBrush}"/>
    </Style>

    <!--  Pressed state  -->
    <Style Selector="^:pressed /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
      <Setter Property="TextElement.Foreground" Value="{DynamicResource BrowserTextBrush}"/>
    </Style>

    <!--  Selected Pressed state  -->
    <Style Selector="^:selected:pressed /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{DynamicResource BrowserSelectionBrush}"/>
      <Setter Property="TextElement.Foreground" Value="{DynamicResource BrowserSelectionTextBrush}"/>
    </Style>

    <!--  Disabled state  -->
    <Style Selector="^:disabled /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
      <Setter Property="TextElement.Foreground" Value="{DynamicResource BrowserDisabledBrush}"/>
    </Style>
  <Style Selector="^:selected"><Setter Property="Background" Value="{DynamicResource BrowserInputBrush}"/><Setter Property="Foreground" Value="{DynamicResource BrowserTextBrush}"/><Setter Property="FontWeight" Value="SemiBold"/></Style><Style Selector="^:selected /template/ Border#PART_SelectedPipe"><Setter Property="Opacity" Value="0"/></Style><Style Selector="^:selected:pointerover /template/ Border#PART_LayoutRoot"><Setter Property="Background" Value="{DynamicResource BrowserHoverBrush}"/><Setter Property="TextElement.Foreground" Value="{DynamicResource BrowserTextBrush}"/></Style></ControlTheme>
</ResourceDictionary>
