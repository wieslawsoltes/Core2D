<!-- Derived from AvaloniaUI/Avalonia 11.3.12 (MIT). See LICENSE.Avalonia.txt and UpstreamManifest.json. --><ResourceDictionary xmlns="https://github.com/avaloniaui" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:ClassModifier="internal">
  <Design.PreviewWith>
    <Border Padding="20">
      <StackPanel Spacing="20">
        <CheckBox>Unchecked</CheckBox>
        <CheckBox IsChecked="True">Checked</CheckBox>
        <CheckBox IsThreeState="True" IsChecked="{x:Null}">Indeterminate</CheckBox>
        <CheckBox Width="120">Checkbox should wrap its text</CheckBox>
      </StackPanel>
    </Border>
  </Design.PreviewWith>

  <StreamGeometry x:Key="CheckMarkPathData">M5.5 10.586 1.707 6.793A1 1 0 0 0 .293 8.207l4.5 4.5a1 1 0 0 0 1.414 0l11-11A1 1 0 0 0 15.793.293L5.5 10.586Z</StreamGeometry>
  <x:Double x:Key="CheckBoxMinHeight">32</x:Double> 
  
  <ControlTheme x:Key="{x:Type CheckBox}" TargetType="CheckBox" x:DataType="CheckBox"><Setter Property="FontFamily" Value="{DynamicResource BrowserFontFamily}"/><Setter Property="FontSize" Value="{DynamicResource BrowserFontSize}"/>
    <Setter Property="Padding" Value="8,0,0,0"/>
    <Setter Property="HorizontalAlignment" Value="Left"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
    <Setter Property="HorizontalContentAlignment" Value="Left"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}"/>
    <Setter Property="MinHeight" Value="{DynamicResource BrowserControlHeight}"/>
    <Setter Property="Foreground" Value="{DynamicResource BrowserTextBrush}"/>
    <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
    <Setter Property="Template">
      <ControlTemplate>
        <Grid x:Name="RootGrid" ColumnDefinitions="20,*">
          <Border x:Name="PART_Border" Grid.ColumnSpan="2" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}"/>

          <Grid VerticalAlignment="Top" Height="{DynamicResource CheckBoxMinHeight}">
            <Border x:Name="NormalRectangle" BorderBrush="{DynamicResource BrowserBorderHoverBrush}" Background="{DynamicResource BrowserInputBrush}" BorderThickness="{DynamicResource CheckBoxBorderThemeThickness}" CornerRadius="{TemplateBinding CornerRadius}" UseLayoutRounding="False" Height="20" Width="20"/>

            <Viewbox UseLayoutRounding="False">
              <Panel>
                <Panel Height="16" Width="16"/>
                <Path x:Name="CheckGlyph" Opacity="0" Fill="{DynamicResource BrowserTransparentBrush}" Stretch="Uniform" VerticalAlignment="Center" FlowDirection="LeftToRight"/>
              </Panel>
            </Viewbox>
          </Grid>
          <ContentPresenter x:Name="PART_ContentPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" TextWrapping="Wrap" Grid.Column="1"/>
        </Grid>
      </ControlTemplate>
    </Setter>

    <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="RecognizesAccessKey" Value="True"/>
    </Style>

    <!-- Unchecked PointerOver State -->
    <Style Selector="^:pointerover">
      <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource BrowserTextBrush}"/>
      </Style>

      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
      </Style>

      <Style Selector="^ /template/ Border#NormalRectangle">
        <Setter Property="BorderBrush" Value="{DynamicResource BrowserBorderHoverBrush}"/>
        <Setter Property="Background" Value="{DynamicResource BrowserInputBrush}"/>
      </Style>

      <Style Selector="^ /template/ Path#CheckGlyph">
        <Setter Property="Fill" Value="{DynamicResource BrowserTransparentBrush}"/>
      </Style>
    </Style>

    <!-- Unchecked Pressed State -->
    <Style Selector="^:pressed">
      <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource BrowserTextBrush}"/>
      </Style>

      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
      </Style>

      <Style Selector="^ /template/ Border#NormalRectangle">
        <Setter Property="BorderBrush" Value="{DynamicResource BrowserBorderHoverBrush}"/>
        <Setter Property="Background" Value="{DynamicResource BrowserInputBrush}"/>
      </Style>

      <Style Selector="^ /template/ Path#CheckGlyph">
        <Setter Property="Fill" Value="{DynamicResource BrowserTransparentBrush}"/>
      </Style>
    </Style>

    <!-- Unchecked Disabled state -->
    <Style Selector="^:disabled">
      <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource BrowserDisabledBrush}"/>
      </Style>

      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
      </Style>

      <Style Selector="^ /template/ Border#NormalRectangle">
        <Setter Property="BorderBrush" Value="{DynamicResource BrowserInputBrush}"/>
        <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
      </Style>

      <Style Selector="^ /template/ Path#CheckGlyph">
        <Setter Property="Fill" Value="{DynamicResource BrowserDisabledBrush}"/>
      </Style>
    </Style>

    <Style Selector="^:checked">
      <!-- Checked Normal State -->
      <Setter Property="Foreground" Value="{DynamicResource BrowserSelectionTextBrush}"/>
      <Setter Property="Background" Value="{DynamicResource BrowserSelectionBrush}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>

      <Style Selector="^ /template/ Border#NormalRectangle">
        <Setter Property="BorderBrush" Value="{DynamicResource BrowserAccentBrush}"/>
        <Setter Property="Background" Value="{DynamicResource BrowserAccentBrush}"/>
      </Style>

      <Style Selector="^ /template/ Path#CheckGlyph">
        <Setter Property="Fill" Value="{DynamicResource BrowserOnAccentBrush}"/>
        <Setter Property="Data" Value="{StaticResource CheckMarkPathData}"/>
        <Setter Property="Width" Value="9"/>
        <Setter Property="Opacity" Value="1"/>
      </Style>

      <!-- Checked PointerOver State -->
      <Style Selector="^:pointerover">
        <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
          <Setter Property="Foreground" Value="{DynamicResource BrowserSelectionTextBrush}"/>
        </Style>

        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource BrowserSelectionBrush}"/>
          <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
        </Style>

        <Style Selector="^ /template/ Border#NormalRectangle">
          <Setter Property="BorderBrush" Value="{DynamicResource BrowserAccentBrush}"/>
          <Setter Property="Background" Value="{DynamicResource BrowserAccentBrush}"/>
        </Style>

        <Style Selector="^ /template/ Path#CheckGlyph">
          <Setter Property="Fill" Value="{DynamicResource BrowserOnAccentBrush}"/>
        </Style>
      </Style>

      <!-- Checked Pressed State -->
      <Style Selector="^:pressed">
        <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
          <Setter Property="Foreground" Value="{DynamicResource BrowserSelectionTextBrush}"/>
        </Style>

        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource BrowserSelectionBrush}"/>
          <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
        </Style>

        <Style Selector="^ /template/ Border#NormalRectangle">
          <Setter Property="BorderBrush" Value="{DynamicResource BrowserAccentBrush}"/>
          <Setter Property="Background" Value="{DynamicResource BrowserAccentBrush}"/>
        </Style>

        <Style Selector="^ /template/ Path#CheckGlyph">
          <Setter Property="Fill" Value="{DynamicResource BrowserOnAccentBrush}"/>
        </Style>
      </Style>

      <!-- Checked Disabled State -->
      <Style Selector="^:disabled">
        <Style Selector="^ ContentPresenter#PART_ContentPresenter">
          <Setter Property="Foreground" Value="{DynamicResource BrowserDisabledBrush}"/>
        </Style>

        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
          <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
        </Style>

        <Style Selector="^ /template/ Border#NormalRectangle">
          <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
          <Setter Property="Background" Value="{DynamicResource BrowserInputBrush}"/>
        </Style>

        <Style Selector="^ /template/ Path#CheckGlyph">
          <Setter Property="Fill" Value="{DynamicResource BrowserDisabledBrush}"/>
        </Style>
      </Style>
    </Style>

    <Style Selector="^:indeterminate">
      <!-- Indeterminate Normal State -->
      <Setter Property="Foreground" Value="{DynamicResource BrowserTextBrush}"/>
      <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>

      <Style Selector="^ /template/ Border#NormalRectangle">
        <Setter Property="BorderBrush" Value="{DynamicResource BrowserAccentBrush}"/>
        <Setter Property="Background" Value="{DynamicResource BrowserAccentBrush}"/>
      </Style>

      <Style Selector="^ /template/ Path#CheckGlyph">
        <Setter Property="Fill" Value="{DynamicResource BrowserOnAccentBrush}"/>
        <Setter Property="Data" Value="M1536 1536v-1024h-1024v1024h1024z"/>
        <Setter Property="Width" Value="7"/>
        <Setter Property="Opacity" Value="1"/>
      </Style>

      <!-- Indeterminate PointerOver State -->
      <Style Selector="^:pointerover">
        <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
          <Setter Property="Foreground" Value="{DynamicResource BrowserTextBrush}"/>
        </Style>

        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
          <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
        </Style>

        <Style Selector="^ /template/ Border#NormalRectangle">
          <Setter Property="BorderBrush" Value="{DynamicResource BrowserAccentBrush}"/>
          <Setter Property="Background" Value="{DynamicResource BrowserAccentBrush}"/>
        </Style>

        <Style Selector="^ /template/ Path#CheckGlyph">
          <Setter Property="Fill" Value="{DynamicResource BrowserOnAccentBrush}"/>
        </Style>
      </Style>

      <!-- Indeterminate Pressed State -->
      <Style Selector="^:pressed">
        <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
          <Setter Property="Foreground" Value="{DynamicResource BrowserTextBrush}"/>
        </Style>

        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
          <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
        </Style>

        <Style Selector="^ /template/ Border#NormalRectangle">
          <Setter Property="BorderBrush" Value="{DynamicResource BrowserAccentBrush}"/>
          <Setter Property="Background" Value="{DynamicResource BrowserAccentBrush}"/>
        </Style>

        <Style Selector="^ /template/ Path#CheckGlyph">
          <Setter Property="Fill" Value="{DynamicResource BrowserOnAccentBrush}"/>
        </Style>
      </Style>

      <!-- Indeterminate Disabled State -->
      <Style Selector="^:disabled">
        <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
          <Setter Property="Foreground" Value="{DynamicResource BrowserDisabledBrush}"/>
        </Style>

        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
          <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
        </Style>

        <Style Selector="^ /template/ Border#NormalRectangle">
          <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
          <Setter Property="Background" Value="{DynamicResource BrowserInputBrush}"/>
        </Style>

        <Style Selector="^ /template/ Path#CheckGlyph">
          <Setter Property="Fill" Value="{DynamicResource BrowserDisabledBrush}"/>
        </Style>
      </Style>
    </Style>
  </ControlTheme>
</ResourceDictionary>
