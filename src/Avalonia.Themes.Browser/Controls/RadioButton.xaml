<!-- Derived from AvaloniaUI/Avalonia 11.3.12 (MIT). See LICENSE.Avalonia.txt and UpstreamManifest.json. --><ResourceDictionary xmlns="https://github.com/avaloniaui" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:ClassModifier="internal">
  <Design.PreviewWith>
    <Border Padding="20">
      <StackPanel Spacing="10">
        <RadioButton Content="Option 1" Background="Green"/>
        <RadioButton Content="Option 2"/>
        <RadioButton IsEnabled="False" Content="Option 3"/>
        <RadioButton Content="Option 2"/>
      </StackPanel>
    </Border>
  </Design.PreviewWith>
  
  <x:Double x:Key="RadioButtonMinHeight">32</x:Double>
  
  <ControlTheme x:Key="{x:Type RadioButton}" TargetType="RadioButton" x:DataType="RadioButton"><Setter Property="FontFamily" Value="{DynamicResource BrowserFontFamily}"/><Setter Property="FontSize" Value="{DynamicResource BrowserFontSize}"/><Setter Property="MinHeight" Value="{DynamicResource BrowserControlHeight}"/>
    <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource BrowserTextBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
    <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}"/>
    <Setter Property="Padding" Value="8,0,0,0"/>
    <Setter Property="HorizontalAlignment" Value="Left"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
    <Setter Property="HorizontalContentAlignment" Value="Left"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <ControlTemplate TargetType="RadioButton">
        <Border Name="RootBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}">
          <Grid ColumnDefinitions="20,*">
            <Grid Height="{DynamicResource RadioButtonMinHeight}" VerticalAlignment="Top">

              <Ellipse Name="OuterEllipse" Width="20" Height="20" Fill="{DynamicResource BrowserInputBrush}" Stroke="{DynamicResource BrowserBorderHoverBrush}" StrokeThickness="{DynamicResource RadioButtonBorderThemeThickness}" UseLayoutRounding="False"/>

              <Ellipse Name="CheckOuterEllipse" Width="20" Height="20" Fill="{DynamicResource BrowserAccentBrush}" Opacity="0" Stroke="{DynamicResource BrowserAccentBrush}" StrokeThickness="{DynamicResource RadioButtonBorderThemeThickness}" UseLayoutRounding="False"/>

              <Ellipse Name="CheckGlyph" Width="8" Height="8" Fill="{DynamicResource BrowserAccentBrush}" Opacity="0" Stroke="{DynamicResource BrowserAccentBrush}" UseLayoutRounding="False"/>
            </Grid>

            <ContentPresenter Name="PART_ContentPresenter" Grid.Column="1" Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" Foreground="{TemplateBinding Foreground}"/>
          </Grid>
        </Border>
      </ControlTemplate>
    </Setter>

    <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="RecognizesAccessKey" Value="True"/>
    </Style>

    <!--  PointerOver State  -->
    <Style Selector="^:pointerover /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Foreground" Value="{DynamicResource BrowserTextBrush}"/>
    </Style>

    <Style Selector="^:pointerover /template/ Border#RootBorder">
      <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
    </Style>

    <Style Selector="^:pointerover /template/ Ellipse#OuterEllipse">
      <Setter Property="Stroke" Value="{DynamicResource BrowserBorderHoverBrush}"/>
      <Setter Property="Fill" Value="{DynamicResource BrowserInputBrush}"/>
    </Style>

    <Style Selector="^:pointerover /template/ Ellipse#CheckOuterEllipse">
      <Setter Property="Stroke" Value="{DynamicResource BrowserAccentBrush}"/>
      <Setter Property="Fill" Value="{DynamicResource BrowserAccentBrush}"/>
    </Style>

    <Style Selector="^:pointerover /template/ Ellipse#CheckGlyph">
      <Setter Property="Stroke" Value="{DynamicResource BrowserAccentBrush}"/>
      <Setter Property="Fill" Value="{DynamicResource BrowserAccentBrush}"/>
    </Style>


    <!--  Pressed State  -->
    <Style Selector="^:pressed /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Foreground" Value="{DynamicResource BrowserTextBrush}"/>
    </Style>

    <Style Selector="^:pressed /template/ Border#RootBorder">
      <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
    </Style>

    <Style Selector="^:pressed /template/ Ellipse#OuterEllipse">
      <Setter Property="Stroke" Value="{DynamicResource BrowserBorderHoverBrush}"/>
      <Setter Property="Fill" Value="{DynamicResource BrowserInputBrush}"/>
    </Style>

    <Style Selector="^:pressed /template/ Ellipse#CheckOuterEllipse">
      <Setter Property="Stroke" Value="{DynamicResource BrowserAccentBrush}"/>
      <Setter Property="Fill" Value="{DynamicResource BrowserAccentBrush}"/>
    </Style>

    <Style Selector="^:pressed /template/ Ellipse#CheckGlyph">
      <Setter Property="Stroke" Value="{DynamicResource BrowserAccentBrush}"/>
      <Setter Property="Fill" Value="{DynamicResource BrowserAccentBrush}"/>
    </Style>


    <!--  Disabled State  -->
    <Style Selector="^:disabled /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Foreground" Value="{DynamicResource BrowserDisabledBrush}"/>
    </Style>

    <Style Selector="^:disabled /template/ Border#RootBorder">
      <Setter Property="Background" Value="{DynamicResource BrowserTransparentBrush}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BrowserTransparentBrush}"/>
    </Style>

    <Style Selector="^:disabled /template/ Ellipse#OuterEllipse">
      <Setter Property="Stroke" Value="{DynamicResource BrowserBorderBrush}"/>
      <Setter Property="Fill" Value="{DynamicResource BrowserTransparentBrush}"/>
    </Style>

    <Style Selector="^:disabled /template/ Ellipse#CheckOuterEllipse">
      <Setter Property="Stroke" Value="{DynamicResource BrowserBorderBrush}"/>
      <Setter Property="Fill" Value="{DynamicResource BrowserInputBrush}"/>
    </Style>

    <Style Selector="^:disabled /template/ Ellipse#CheckGlyph">
      <Setter Property="Stroke" Value="{DynamicResource BrowserDisabledBrush}"/>
      <Setter Property="Fill" Value="{DynamicResource BrowserDisabledBrush}"/>
    </Style>


    <!--  Checked State  -->
    <Style Selector="^:checked /template/ Ellipse#CheckGlyph">
      <Setter Property="Opacity" Value="1"/>
    </Style>

    <Style Selector="^:checked /template/ Ellipse#OuterEllipse">
      <Setter Property="Opacity" Value="0"/>
    </Style>

    <Style Selector="^:checked /template/ Ellipse#CheckOuterEllipse">
      <Setter Property="Opacity" Value="1"/>
    </Style>
  </ControlTheme>
</ResourceDictionary>
