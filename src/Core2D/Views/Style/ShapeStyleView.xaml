<UserControl x:Class="Core2D.Views.Style.ShapeStyleView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:Core2D.Converters;assembly=Core2D"
             xmlns:Style="clr-namespace:Core2D.Style;assembly=Core2D"
             xmlns:Editor="clr-namespace:Core2D.Editor;assembly=Core2D"
             xmlns:Designer="clr-namespace:Core2D.Designer;assembly=Core2D"
             mc:Ignorable="d"
             d:DataContext="{x:Static Designer:DesignerContext.Style}"
             d:DesignWidth="250" d:DesignHeight="400"
             x:DataType="Style:ShapeStyle" x:CompileBindings="True">
    <StackPanel>
        <StackPanel>
            <Label Content="Name" Margin="{DynamicResource TextControlThemePadding}" />
            <TextBox Text="{Binding Name, Mode=TwoWay}" />
        </StackPanel>
        <Expander Header="Stroke" IsExpanded="True">
            <StackPanel>
                <StackPanel>
                    <Label Content="Thickness" Margin="{DynamicResource TextControlThemePadding}" />
                    <TextBox Text="{Binding Thickness, Mode=TwoWay}" x:DataType="Style:BaseStyle" />
                    <Label Content="Line Cap" Margin="{DynamicResource TextControlThemePadding}" />
                    <ComboBox Items="{x:Static Editor:Enums.LineCapValues}" SelectedItem="{Binding LineCap, Mode=TwoWay}" x:DataType="Style:BaseStyle" HorizontalAlignment="Stretch" />
                    <Label Content="Dashes" Margin="{DynamicResource TextControlThemePadding}" />
                    <TextBox Text="{Binding Dashes, Mode=TwoWay}" x:DataType="Style:BaseStyle" />
                    <Label Content="Dash Offset" Margin="{DynamicResource TextControlThemePadding}" />
                    <TextBox Text="{Binding DashOffset, Mode=TwoWay}" x:DataType="Style:BaseStyle" />
                </StackPanel>
                <ContentControl Content="{Binding Stroke}" x:DataType="Style:BaseStyle" />
                <Expander Header="Start Arrow" IsExpanded="False">
                    <ContentControl Content="{Binding StartArrowStyle}" />
                </Expander>
                <Expander Header="End Arrow" IsExpanded="False">
                    <ContentControl Content="{Binding EndArrowStyle}" />
                </Expander>
            </StackPanel>
        </Expander>
        <Expander Header="Fill" IsExpanded="True">
            <ContentControl Content="{Binding Fill}" x:DataType="Style:BaseStyle" />
        </Expander>
        <Expander Header="Text" IsExpanded="False">
            <ContentControl Content="{Binding TextStyle}" />
        </Expander>
    </StackPanel>
</UserControl>
