<UserControl x:Class="Core2D.Views.Shapes.LineShapeView"
             xmlns="https://github.com/avaloniaui" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:s="using:Core2D.Controls.Studio" xmlns:vm="using:Core2D.ViewModels.Shapes"

             x:DataType="vm:LineShapeViewModel" x:CompileBindings="True">
  <StackPanel Spacing="10">
    <s:StudioNameField Value="{Binding Name, Mode=TwoWay}" AutomationProperties.Name="Object name" />
    <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
      <s:StudioSwitch Content="Fill" IsChecked="{Binding IsFilled, Mode=TwoWay}" />
      <s:StudioSwitch Grid.Column="1" Content="Stroke" IsChecked="{Binding IsStroked, Mode=TwoWay}" />
    </Grid>
    <s:StudioSection Header="Geometry">
      <StackPanel Spacing="4">
        <s:StudioPropertyField Label="Start"><s:StudioCoordinateEditor Source="{Binding Start}" /></s:StudioPropertyField>
        <s:StudioPropertyField Label="End"><s:StudioCoordinateEditor Source="{Binding End}" /></s:StudioPropertyField>
      </StackPanel>
    </s:StudioSection>
  </StackPanel>
</UserControl>
