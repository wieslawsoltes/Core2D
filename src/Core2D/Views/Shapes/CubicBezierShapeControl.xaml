<UserControl x:Class="Core2D.Views.Shapes.CubicBezierShapeControl"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:Core2D.Converters;assembly=Core2D"
             xmlns:Shapes="clr-namespace:Core2D.Shapes;assembly=Core2D"
             xmlns:Designer="clr-namespace:Core2D.Designer;assembly=Core2D"
             mc:Ignorable="d"
             d:DataContext="{x:Static Designer:DesignerContext.CubicBezier}"
             d:DesignWidth="250" d:DesignHeight="400"
             x:DataType="Shapes:CubicBezierShape" x:CompileBindings="True">
    <StackPanel>
        <Expander Header="CubicBezier">
            <StackPanel>
                <Label Content="Name" Margin="{DynamicResource TextControlThemePadding}" />
                <TextBox Text="{Binding Name, Mode=TwoWay}" />
                <CheckBox Content="IsStroked" IsChecked="{Binding IsStroked, Mode=TwoWay}" HorizontalAlignment="Stretch" />
                <CheckBox Content="IsFilled" IsChecked="{Binding IsFilled, Mode=TwoWay}" HorizontalAlignment="Stretch" />
            </StackPanel>
        </Expander>
        <Expander Header="Point1">
            <ContentControl Content="{Binding Point1}" />
        </Expander>
        <Expander Header="Point2">
            <ContentControl Content="{Binding Point2}" />
        </Expander>
        <Expander Header="Point3">
            <ContentControl Content="{Binding Point3}" />
        </Expander>
        <Expander Header="Point4">
            <ContentControl Content="{Binding Point4}" />
        </Expander>
        <Expander Header="Data">
            <ContentControl Content="{Binding Data}" />
        </Expander>
        <Expander Header="Style">
            <ContentControl Content="{Binding Style}" />
        </Expander>
        <Expander Header="State">
            <ContentControl Content="{Binding State}" />
        </Expander>
    </StackPanel>
</UserControl>
