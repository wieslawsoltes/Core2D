<UserControl x:Class="Core2D.UI.Views.Renderer.ShapeStateControl"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:Core2D.UI.Converters;assembly=Core2D.UI"
             xmlns:Designer="clr-namespace:Core2D.UI.Designer;assembly=Core2D.UI"
             mc:Ignorable="d"
             d:DataContext="{x:Static Designer:DesignerContext.State}"
             d:DesignWidth="250" d:DesignHeight="400">
    <StackPanel>
        <CheckBox Content="Default" IsChecked="{Binding Default, Mode=TwoWay}"/>
        <CheckBox Content="Visible" IsChecked="{Binding Visible, Mode=TwoWay}"/>
        <CheckBox Content="Printable" IsChecked="{Binding Printable, Mode=TwoWay}"/>
        <CheckBox Content="Locked" IsChecked="{Binding Locked, Mode=TwoWay}"/>
        <CheckBox Content="Size" IsChecked="{Binding Size, Mode=TwoWay}"/>
        <CheckBox Content="Thickness" IsChecked="{Binding Thickness, Mode=TwoWay}"/>
        <CheckBox Content="Connector" IsChecked="{Binding Connector, Mode=TwoWay}"/>
        <CheckBox Content="None" IsChecked="{Binding None, Mode=TwoWay}"/>
        <CheckBox Content="Standalone" IsChecked="{Binding Standalone, Mode=TwoWay}"/>
        <CheckBox Content="Input" IsChecked="{Binding Input, Mode=TwoWay}"/>
        <CheckBox Content="Output" IsChecked="{Binding Output, Mode=TwoWay}"/>
    </StackPanel>
</UserControl>
