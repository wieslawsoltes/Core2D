<UserControl x:Class="Core2D.UI.Views.Path.QuadraticBezierSegmentControl"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:Core2D.UI.Converters;assembly=Core2D.UI"
             xmlns:Designer="clr-namespace:Core2D.UI.Designer;assembly=Core2D.UI"
             mc:Ignorable="d"
             d:DataContext="{x:Static Designer:DesignerContext.QuadraticBezierSegment}"
             d:DesignWidth="250" d:DesignHeight="400">
    <StackPanel>
        <CheckBox Content="IsStroked" IsChecked="{Binding IsStroked, Mode=TwoWay}" HorizontalAlignment="Stretch"/>
        <CheckBox Content="IsSmoothJoin" IsChecked="{Binding IsSmoothJoin, Mode=TwoWay}" HorizontalAlignment="Stretch"/>
        <TabControl>
            <TabItem Header="Point1">
                <ContentControl Content="{Binding Point1}"/>
            </TabItem>
            <TabItem Header="Point2">
                <ContentControl Content="{Binding Point2}"/>
            </TabItem>
        </TabControl>
    </StackPanel>
</UserControl>
