<!-- Copyright (c) Wiesław Šoltés. All rights reserved. -->
<!-- Licensed under the MIT license. See LICENSE file in the project root for full license information. -->
<UserControl x:Class="Core2D.Perspex.Controls.Project.StylesControl"
             xmlns="https://github.com/perspex"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="clr-namespace:Perspex.Xaml.Interactivity;assembly=Perspex.Xaml.Interactivity"
             xmlns:ia="clr-namespace:Perspex.Xaml.Interactions.Core;assembly=Perspex.Xaml.Interactions"
             xmlns:local="clr-namespace:Core2D.Perspex;assembly=Core2D.Perspex"
             xmlns:converters="clr-namespace:Core2D.Perspex.Converters;assembly=Core2D.Perspex"
             xmlns:Editor="clr-namespace:Core2D.Editor;assembly=Core2D"
             xmlns:Style="clr-namespace:Core2D.Style;assembly=Core2D"
             xmlns:Designer="clr-namespace:Core2D.Editor.Designer;assembly=Core2D"
             Design.DataContext="{Static Designer:DesignerContext.Project}"
             Design.Width="250" Design.Height="400">
    <Grid RowDefinitions="Auto,Auto,Auto,*">
        <Grid RowDefinitions="*" ColumnDefinitions="*,Auto" Grid.Row="0">
            <TextBox Text="{Binding CurrentStyleLibrary.Name, Mode=TwoWay}" Classes="left" Grid.Column="0"/>
            <Menu Grid.Column="1" HorizontalAlignment="Right">
                <MenuItem Header="+" Command="{Static Editor:Commands.AddStyleLibraryCommand}"/>
                <MenuItem Header="-" Command="{Static Editor:Commands.RemoveStyleLibraryCommand}" CommandParameter="{Binding CurrentStyleLibrary}"/>
            </Menu>
        </Grid>
        <DropDown Items="{Binding StyleLibraries}" SelectedItem="{Binding CurrentStyleLibrary, Mode=TwoWay}" Grid.Row="1" Classes="default">
            <DropDown.DataTemplates>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" Classes="default"/>
                </DataTemplate>
            </DropDown.DataTemplates>
        </DropDown>
        <Menu Grid.Row="2" HorizontalAlignment="Right">
            <MenuItem Header="apply" Command="{Static Editor:Commands.ApplyStyleCommand}" CommandParameter="{Binding CurrentStyleLibrary.Selected}"/>
            <MenuItem Header="+" Command="{Static Editor:Commands.AddStyleCommand}" CommandParameter="{Binding CurrentStyleLibrary}"/>
            <MenuItem Header="-" Command="{Static Editor:Commands.RemoveStyleCommand}" CommandParameter="{Binding CurrentStyleLibrary.Selected}"/>
        </Menu>
        <ListBox Name="styles" Items="{Binding CurrentStyleLibrary.Items}" SelectedItem="{Binding CurrentStyleLibrary.Selected, Mode=TwoWay}" Grid.Row="3" Classes="default">
            <ListBox.KeyBindings>
                <KeyBinding Command="{Static Editor:Commands.AddStyleCommand}" CommandParameter="{Binding #styles.DataContext.CurrentStyleLibrary, Mode=OneWay}" Gesture="Ctrl+N"/>
                <KeyBinding Command="{Static Editor:Commands.RemoveStyleCommand}" CommandParameter="{Binding #styles.SelectedItem}" Gesture="Delete"/>
                <KeyBinding Command="{Static Editor:Commands.ExportStyleCommand}" CommandParameter="{Binding #styles.SelectedItem}" Gesture="Ctrl+E"/>
            </ListBox.KeyBindings>
            <ListBox.DataTemplates>
                <DataTemplate DataType="Style:ShapeStyle">
                    <TextBlock Name="style" Text="{Binding Name}" Classes="default">
                        <i:Interaction.Behaviors>
                            <ia:EventTriggerBehavior EventName="DoubleTapped" SourceObject="{Binding #style}">
                                <ia:InvokeCommandAction Command="{Static Editor:Commands.ApplyStyleCommand}" CommandParameter="{Binding Mode=OneWay}"/>
                            </ia:EventTriggerBehavior>
                        </i:Interaction.Behaviors>
                        <TextBlock.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="_Delete" Command="{Static Editor:Commands.RemoveStyleCommand}" CommandParameter="{Binding Mode=OneWay}"/>
                                <Separator/>
                                <MenuItem Header="E_xport" Command="{Static Editor:Commands.ExportStyleCommand}" CommandParameter="{Binding Mode=OneWay}"/>
                            </ContextMenu>
                        </TextBlock.ContextMenu>
                    </TextBlock>
                </DataTemplate>
            </ListBox.DataTemplates>
        </ListBox>
    </Grid>
</UserControl>
