<UserControl x:Class="Test.Controls.StyleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <GroupBox Header="Name">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding Name}" TextAlignment="Left" VerticalAlignment="Center" Margin="1" BorderBrush="{x:Null}"/>
            </Grid>
        </GroupBox>
        <GroupBox Header="Stroke">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="A" Margin="0,0,2,0" Grid.Row="0" Grid.Column="0" TextAlignment="Right" VerticalAlignment="Center"/>
                <TextBlock Text="R" Margin="0,0,2,0" Grid.Row="1" Grid.Column="0" TextAlignment="Right" VerticalAlignment="Center"/>
                <TextBlock Text="G" Margin="0,0,2,0" Grid.Row="2" Grid.Column="0" TextAlignment="Right" VerticalAlignment="Center"/>
                <TextBlock Text="B" Margin="0,0,2,0" Grid.Row="3" Grid.Column="0" TextAlignment="Right" VerticalAlignment="Center"/>
                <Slider Value="{Binding Stroke.A}" Minimum="0" Maximum="255" TickFrequency="1" IsSnapToTickEnabled="True" Grid.Row="0" Grid.Column="1" Margin="1"/>
                <Slider Value="{Binding Stroke.R}" Minimum="0" Maximum="255" TickFrequency="1" IsSnapToTickEnabled="True" Grid.Row="1" Grid.Column="1" Margin="1"/>
                <Slider Value="{Binding Stroke.G}" Minimum="0" Maximum="255" TickFrequency="1" IsSnapToTickEnabled="True" Grid.Row="2" Grid.Column="1" Margin="1"/>
                <Slider Value="{Binding Stroke.B}" Minimum="0" Maximum="255" TickFrequency="1" IsSnapToTickEnabled="True" Grid.Row="3" Grid.Column="1" Margin="1"/>
                <TextBox Text="{Binding Stroke.A}" BorderBrush="{x:Null}" Grid.Row="0" Grid.Column="2" TextAlignment="Center" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Stroke.R}" BorderBrush="{x:Null}" Grid.Row="1" Grid.Column="2" TextAlignment="Center" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Stroke.G}" BorderBrush="{x:Null}" Grid.Row="2" Grid.Column="2" TextAlignment="Center" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Stroke.B}" BorderBrush="{x:Null}" Grid.Row="3" Grid.Column="2" TextAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </GroupBox>
        <GroupBox Header="Fill">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="A" Margin="0,0,2,0" Grid.Row="0" Grid.Column="0" TextAlignment="Right" VerticalAlignment="Center"/>
                <TextBlock Text="R" Margin="0,0,2,0" Grid.Row="1" Grid.Column="0" TextAlignment="Right" VerticalAlignment="Center"/>
                <TextBlock Text="G" Margin="0,0,2,0" Grid.Row="2" Grid.Column="0" TextAlignment="Right" VerticalAlignment="Center"/>
                <TextBlock Text="B" Margin="0,0,2,0" Grid.Row="3" Grid.Column="0" TextAlignment="Right" VerticalAlignment="Center"/>
                <Slider Value="{Binding Fill.A}" Minimum="0" Maximum="255" TickFrequency="1" IsSnapToTickEnabled="True" Grid.Row="0" Grid.Column="1" Margin="1"/>
                <Slider Value="{Binding Fill.R}" Minimum="0" Maximum="255" TickFrequency="1" IsSnapToTickEnabled="True" Grid.Row="1" Grid.Column="1" Margin="1"/>
                <Slider Value="{Binding Fill.G}" Minimum="0" Maximum="255" TickFrequency="1" IsSnapToTickEnabled="True" Grid.Row="2" Grid.Column="1" Margin="1"/>
                <Slider Value="{Binding Fill.B}" Minimum="0" Maximum="255" TickFrequency="1" IsSnapToTickEnabled="True" Grid.Row="3" Grid.Column="1" Margin="1"/>
                <TextBox Text="{Binding Fill.A}" BorderBrush="{x:Null}" Grid.Row="0" Grid.Column="2" TextAlignment="Center" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Fill.R}" BorderBrush="{x:Null}" Grid.Row="1" Grid.Column="2" TextAlignment="Center" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Fill.G}" BorderBrush="{x:Null}" Grid.Row="2" Grid.Column="2" TextAlignment="Center" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Fill.B}" BorderBrush="{x:Null}" Grid.Row="3" Grid.Column="2" TextAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </GroupBox>
        <GroupBox Header="Thickness">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <Slider Value="{Binding Thickness}" Minimum="0" Maximum="20" TickFrequency="0.1" SmallChange="0.1" LargeChange="1" IsSnapToTickEnabled="True" Grid.Row="0" Grid.Column="0" Margin="1"/>
                <TextBox Text="{Binding Thickness}" BorderBrush="{x:Null}" Grid.Row="0" Grid.Column="1" TextAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </GroupBox>
    </StackPanel>
</UserControl>
