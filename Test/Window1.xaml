<Window x:Class="Test.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Test.Controls"
        Title="Test" 
        Height="720" Width="1080"
        Background="LightGray"
        WindowStartupLocation="CenterScreen" 
        WindowState="Normal"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextHintingMode="Auto"
        TextOptions.TextRenderingMode="Auto">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="180"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Background="Transparent">
            <MenuItem Header="_File">
                <MenuItem x:Name="fileExit" Header="E_xit"/>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem x:Name="editClear" Header="_Clear"/>
            </MenuItem>
            <MenuItem Header="_Tool">
                <MenuItem x:Name="toolNone" Header="_None"/>
                <Separator/>
                <MenuItem x:Name="toolLine" Header="_Line"/>
                <MenuItem x:Name="toolRectangle" Header="_Rectangle"/>
                <MenuItem x:Name="toolEllipse" Header="_Ellipse"/>
                <Separator/>
                <MenuItem x:Name="toolBezier" Header="_Bezier"/>
            </MenuItem>
            <MenuItem Header="_Options">
                <MenuItem x:Name="optionsIsFilled" Header="Toggle _Filled"/>
            </MenuItem>
            <MenuItem Header="_Styles">
                <MenuItem x:Name="stylesEdit" Header="_Edit..."/>
            </MenuItem>
        </Menu>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Grid.Row="1" Grid.Column="0" Margin="5,5,0,5">
            <Canvas x:Name="canvas" Width="800" Height="600" Background="DarkGray"/>
        </ScrollViewer>
        <GridSplitter Grid.Row="1" Grid.Column="1" Width="5" Background="Transparent" ResizeBehavior="PreviousAndNext"/>
        <TabControl Grid.Row="1" Grid.Column="2" Margin="0,5,5,5">
            <TabItem Header="Layers">
                <ListBox ItemsSource="{Binding Layers}" SelectedItem="{Binding CurrentLayer}" BorderBrush="{x:Null}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Header="Styles">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ListBox ItemsSource="{Binding Styles}" SelectedItem="{Binding CurrentStyle}" Grid.Row="0" BorderBrush="{x:Null}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <controls:StyleControl DataContext="{Binding CurrentStyle}" Grid.Row="1" Margin="2"/>
                </Grid>
            </TabItem>
            <TabItem Header="Shapes">
                <ListBox ItemsSource="{Binding CurrentLayer.Shapes}" BorderBrush="{x:Null}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
