<Window x:Class="Test.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Test" 
        Height="720" Width="1080"
        Background="LightGray"
        WindowStartupLocation="CenterScreen" 
        WindowState="Normal"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextHintingMode="Auto"
        TextOptions.TextRenderingMode="Auto">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Background="Transparent">
            <MenuItem Header="_File">
                <MenuItem x:Name="fileExit" Header="E_xit"/>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem x:Name="editClear" Header="_Clear"/>
            </MenuItem>
            <MenuItem Header="_Tool">
                <MenuItem x:Name="toolNone" Header="_None"/>
                <Separator/>
                <MenuItem x:Name="toolLine" Header="_Line"/>
                <MenuItem x:Name="toolRectangle" Header="_Rectangle"/>
                <MenuItem x:Name="toolEllipse" Header="_Ellipse"/>
                <Separator/>
                <MenuItem x:Name="toolBezier" Header="_Bezier"/>
            </MenuItem>
            <MenuItem Header="_Options">
                <MenuItem x:Name="optionsIsFilled" Header="Toggle _Filled"/>
            </MenuItem>
            <MenuItem Header="_Styles">
                <MenuItem x:Name="stylesEdit" Header="_Edit..."/>
            </MenuItem>
        </Menu>
        <Canvas x:Name="canvas" Grid.Row="1" Grid.Column="0" Width="800" Height="600" Background="DarkGray"/>
        <TabControl Grid.Row="1" Grid.Column="1">
            <TabItem Header="Layers">
                <ListBox ItemsSource="{Binding Layers}" SelectedItem="{Binding CurrentLayer}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Header="Styles">
                <ListBox ItemsSource="{Binding Styles}" SelectedItem="{Binding CurrentStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Header="Shapes">
                <ListBox ItemsSource="{Binding CurrentLayer.Shapes}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
