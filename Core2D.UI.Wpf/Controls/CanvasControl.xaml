<!-- Copyright (c) Wiesław Šoltés. All rights reserved. -->
<!-- Licensed under the MIT license. See LICENSE file in the project root for full license information. -->
<UserControl x:Class="Core2D.UI.Wpf.Controls.CanvasControl" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:core="clr-namespace:Core2D;assembly=Core2D" 
             xmlns:controls="clr-namespace:Core2D.UI.Wpf.Controls" 
             xmlns:converters="clr-namespace:Core2D.UI.Wpf.Converters" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600" 
             Width="{Binding Editor.Project.CurrentContainer.Template.Width}" 
             Height="{Binding Editor.Project.CurrentContainer.Template.Height}" 
             controls:LayerElement.Renderer="{Binding Editor.Renderers[0]}">
    <UserControl.InputBindings>
        <KeyBinding Command="{x:Static core:Commands.DeleteCommand}" CommandParameter="{Binding}" Key="Delete" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.SendToBackCommand}" Key="OemMinus" Modifiers="Shift"/>
        <KeyBinding Command="{x:Static core:Commands.SendBackwardCommand}" Key="OemMinus" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.BringForwardCommand}" Key="OemPlus" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.BringToFrontCommand}" Key="OemPlus" Modifiers="Shift"/>
        <KeyBinding Command="{x:Static core:Commands.SendToBackCommand}" Key="Subtract" Modifiers="Shift"/>
        <KeyBinding Command="{x:Static core:Commands.SendBackwardCommand}" Key="Subtract" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.BringForwardCommand}" Key="Add" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.BringToFrontCommand}" Key="Add" Modifiers="Shift"/>
        <KeyBinding Command="{x:Static core:Commands.MoveUpCommand}" Key="Up" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.MoveDownCommand}" Key="Down" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.MoveLeftCommand}" Key="Left" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.MoveRightCommand}" Key="Right" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ToolNoneCommand}" Key="N" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ToolSelectionCommand}" Key="S" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ToolPointCommand}" Key="P" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ToolLineCommand}" Key="L" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ToolRectangleCommand}" Key="R" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ToolEllipseCommand}" Key="E" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ToolArcCommand}" Key="A" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ToolBezierCommand}" Key="B" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ToolQBezierCommand}" Key="Q" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ToolTextCommand}" Key="T" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ToolImageCommand}" Key="I" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ToolPathCommand}" Key="H" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ToolMoveCommand}" Key="M" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.DefaultIsStrokedCommand}" Key="K" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.DefaultIsFilledCommand}" Key="F" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.DefaultIsClosedCommand}" Key="D" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.DefaultIsSmoothJoinCommand}" Key="J" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.SnapToGridCommand}" Key="G" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.TryToConnectCommand}" Key="C" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ZoomResetCommand}" Key="Z" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.ZoomExtentCommand}" Key="X" Modifiers=""/>
        <KeyBinding Command="{x:Static core:Commands.DeselectAllCommand}" Key="Esc" Modifiers=""/>
    </UserControl.InputBindings>
    <UserControl.Resources>
        <ControlTemplate x:Key="TemplateControlTemplateKey">
            <ItemsControl ItemsSource="{Binding Layers}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Width="{Binding Width}" Height="{Binding Height}" Background="{Binding Background, Converter={StaticResource ArgbColorToBrushConverter}}"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:LayerElement/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Width" Value="{Binding Path=DataContext.Width, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Canvas}}, FallbackValue=0.0}"/>
                        <Setter Property="Height" Value="{Binding Path=DataContext.Height, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Canvas}}, FallbackValue=0.0}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </ControlTemplate>
        <ControlTemplate x:Key="ContainerControlTemplateKey">
            <ItemsControl ItemsSource="{Binding Layers}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Width="{Binding Template.Width}" Height="{Binding Template.Height}" Background="Transparent"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:LayerElement/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Width" Value="{Binding Path=DataContext.Template.Width, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Canvas}}, FallbackValue=0.0}"/>
                        <Setter Property="Height" Value="{Binding Path=DataContext.Template.Height, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Canvas}}, FallbackValue=0.0}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </ControlTemplate>
    </UserControl.Resources>
    <Border DataContext="{Binding Editor.Project.CurrentContainer}">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Width" Value="{Binding Template.Width}"/>
                <Setter Property="Height" Value="{Binding Template.Height}"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsTemplate}" Value="True">
                        <Setter Property="Width" Value="{Binding Width}"/>
                        <Setter Property="Height" Value="{Binding Height}"/>
                        <Setter Property="BorderBrush" Value="Red"/>
                        <Setter Property="BorderThickness" Value="2"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid Width="{Binding Template.Width}" Height="{Binding Template.Height}" Background="Transparent">
            <Control DataContext="{Binding Template}">
                <Control.Style>
                    <Style TargetType="Control">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Setter Property="Template" Value="{StaticResource TemplateControlTemplateKey}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Setter Property="Template" Value="{x:Null}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Control.Style>
            </Control>
            <Control DataContext="{Binding}" Template="{StaticResource ContainerControlTemplateKey}">
                <Control.Style>
                    <Style TargetType="Control">
                        <Setter Property="Template" Value="{StaticResource ContainerControlTemplateKey}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsTemplate}" Value="True">
                                <Setter Property="Template" Value="{StaticResource TemplateControlTemplateKey}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Control.Style>
            </Control>
            <controls:EditorCanvas Tag="{Binding Path=DataContext, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" Background="Transparent" Focusable="True" FocusVisualStyle="{x:Null}">
                <controls:EditorCanvas.Style>
                    <Style TargetType="{x:Type controls:EditorCanvas}">
                        <Setter Property="Width" Value="{Binding Template.Width}"/>
                        <Setter Property="Height" Value="{Binding Template.Height}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsTemplate}" Value="True">
                                <Setter Property="Width" Value="{Binding Width}"/>
                                <Setter Property="Height" Value="{Binding Height}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </controls:EditorCanvas.Style>
                <controls:LayerElement DataContext="{Binding WorkingLayer}"/>
                <controls:LayerElement DataContext="{Binding HelperLayer}"/>
            </controls:EditorCanvas>
        </Grid>
    </Border>
</UserControl>
