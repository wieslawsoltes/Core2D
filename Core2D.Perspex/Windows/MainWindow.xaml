<!-- Copyright (c) Wiesław Šoltés. All rights reserved. -->
<!-- Licensed under the MIT license. See LICENSE file in the project root for full license information. -->
<Window x:Class="Core2D.Perspex.MainWindow"
        xmlns="https://github.com/perspex"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Core2D.Perspex;assembly=Core2D.Perspex"
        xmlns:converters="clr-namespace:Core2D.Perspex.Converters;assembly=Core2D.Perspex"
        xmlns:Designer="clr-namespace:Core2D.Editor.Designer;assembly=Core2D"
        xmlns:editor="clr-namespace:Core2D.Perspex.Controls.Editor;assembly=Core2D.Perspex"
        Width="1324" Height="670"
        Background="#FFFFFFFF"
        UseLayoutRounding="True"
        Design.DataContext="{Static Designer:DesignerContext.Editor}"
        Design.Width="1200" Design.Height="580">
    <Window.Title>
        <MultiBinding Mode="OneWay" Converter="{Static converters:EditorToTitleMultiConverter.Instance}">
            <Binding Path="Project.Name"/>
            <Binding Path="IsProjectDirty"/>
        </MultiBinding>
    </Window.Title>
    <Grid Name="layout" RowDefinitions="Auto,*,Auto">
        <editor:MenuControl Name="menu" Grid.Row="0"/>
        <editor:EditorControl Name="editor" Grid.Row="1" IsVisible="{Binding Path=Project, Converter={Static converters:IsNotNullConverter.Instance}}"/>
        <editor:DashboardControl Name="dashboard" Grid.Row="1" IsVisible="{Binding Path=Project, Converter={Static converters:IsNullConverter.Instance}}"/>
        <editor:StatusControl Name="status" Grid.Row="2" IsVisible="{Binding Path=Project, Converter={Static converters:IsNotNullConverter.Instance}}"/>
    </Grid>
</Window>
