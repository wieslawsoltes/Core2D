<!-- Copyright (c) Wiesław Šoltés. All rights reserved. -->
<!-- Licensed under the MIT license. See LICENSE file in the project root for full license information. -->
<UserControl x:Class="Core2D.Wpf.Controls.Project.GroupsControl" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Core2D.Wpf" 
             xmlns:converters="clr-namespace:Core2D.Wpf.Converters" 
             xmlns:core="clr-namespace:Core2D;assembly=Core2D" 
             xmlns:custom="clr-namespace:Core2D.Wpf.Controls.Custom"
             xmlns:lists="clr-namespace:Core2D.Wpf.Controls.Custom.Lists"
             mc:Ignorable="d" 
             d:DataContext="{x:Static core:DesignerContext.Context}"
             d:DesignWidth="250" d:DesignHeight="400">
    <Grid>
        <Grid.Resources>
            <ControlTemplate x:Key="GroupLibrariesMenu">
                <Menu Background="Transparent" HorizontalAlignment="Right">
                    <MenuItem Command="{x:Static core:Commands.AddGroupLibraryCommand}" Header="+"/>
                    <MenuItem Command="{x:Static core:Commands.RemoveGroupLibraryCommand}" Header="-"/>
                </Menu>
            </ControlTemplate>
            <ControlTemplate x:Key="GroupsMenu">
                <Menu Background="Transparent" HorizontalAlignment="Right">
                    <MenuItem Command="{x:Static core:Commands.AddGroupCommand}" Header="+"/>
                    <MenuItem Command="{x:Static core:Commands.RemoveGroupCommand}" Header="-"/>
                </Menu>
            </ControlTemplate>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox Text="{Binding Editor.Project.CurrentGroupLibrary.Name, UpdateSourceTrigger=PropertyChanged}" BorderBrush="{x:Null}" TextAlignment="Left" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" Margin="2,0,2,0">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Editor.Project.CurrentGroupLibrary}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Hidden"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>
        <Control Template="{StaticResource GroupLibrariesMenu}" Grid.Row="0" Grid.Column="1" Margin="2,0,2,0"/>
        <ComboBox ItemsSource="{Binding Editor.Project.GroupLibraries}" SelectedItem="{Binding Editor.Project.CurrentGroupLibrary}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="2,2,2,2">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, Delay=500}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <Control Template="{StaticResource GroupsMenu}" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="2,0,2,0"/>
        <lists:XGroupDragAndDropListBox x:Name="groupsListBox" DataContext="{Binding Editor.Project.CurrentGroupLibrary.Items}" Tag="{Binding DataContext.Editor, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" ItemsSource="{Binding}" SelectedItem="{Binding DataContext.Editor.Project.CurrentGroupLibrary.Selected, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" BorderBrush="{x:Null}" HorizontalContentAlignment="Stretch" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2">
            <lists:XGroupDragAndDropListBox.Resources>
                <ContextMenu x:Key="GroupContextMenu" DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                    <MenuItem Header="_Add Group" InputGestureText="Ctrl+N" Command="{x:Static core:Commands.AddGroupCommand}"/>
                    <Separator/>
                    <MenuItem Header="_Delete" InputGestureText="Del" Command="{x:Static core:Commands.RemoveGroupCommand}"/>
                    <Separator/>
                    <MenuItem Header="E_xport" InputGestureText="Ctrl+E" Command="{x:Static core:Commands.ExportGroupCommand}" CommandParameter="{Binding}"/>
                </ContextMenu>
            </lists:XGroupDragAndDropListBox.Resources>
            <lists:XGroupDragAndDropListBox.InputBindings>
                <KeyBinding Command="{x:Static core:Commands.AddGroupCommand}" CommandParameter="{Binding Path=SelectedItem, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type lists:XGroupDragAndDropListBox}}}" Gesture="Ctrl+N"/>
                <KeyBinding Command="{x:Static core:Commands.RemoveGroupCommand}" CommandParameter="{Binding Path=SelectedItem, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type lists:XGroupDragAndDropListBox}}}" Gesture="Del"/>
                <KeyBinding Command="{x:Static core:Commands.ExportGroupCommand}" CommandParameter="{Binding Path=SelectedItem, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type lists:XGroupDragAndDropListBox}}}" Gesture="Ctrl+E"/>
            </lists:XGroupDragAndDropListBox.InputBindings>
            <lists:XGroupDragAndDropListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="ContextMenu" Value="{StaticResource GroupContextMenu}"/>
                </Style>
            </lists:XGroupDragAndDropListBox.ItemContainerStyle>
            <lists:XGroupDragAndDropListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" Margin="0,5,0,5"/>
                </DataTemplate>
            </lists:XGroupDragAndDropListBox.ItemTemplate>
        </lists:XGroupDragAndDropListBox>
    </Grid>
</UserControl>
